<!DOCTYPE html>
<html>
  <head>
    <title>Body Record</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <header class="header">
      <div class="header__bar row">
        <h1 class="grid-6"><a href="/">Body Record</a></h1>
         <% if user_signed_in? %>
          <div class="user_nav grid-6">
            <%= link_to "ログアウト", destroy_user_session_path, method: :delete %>
            <% if @fats = nil %>
              <%= link_to "入力", new_fat_path, class: "post" %>
            <% else %>
              <%= link_to "追加", new_fat_path, class: "post" %>
              <% binding.pry %>
              <%= link_to "変更", edit_body_path(@body.id) %>
              <%= link_to "変更", edit_nutrient_path(@nutrient.id) %>
            <% end %>
          </div>
        <% else %>
          <div class="grid-6">
            <%= link_to "ログイン", new_user_session_path, class: "post" %>
            <%= link_to "新規登録", new_user_registration_path, class: "post" %>
          </div>
        <% end %>
      </div>
    </header>
    <%= yield %>
    <footer>
      <p>
        Copyright BodyRecord 2021.
      </p>
    </footer>
  </body>
</html>
